name: Docker Image

# [临时禁用] 已切换到 Linux 服务器直接部署，Docker 镜像构建暂不自动触发。
# 恢复时把下面的 push 段取消注释即可。
on:
  workflow_dispatch:
    inputs:
      ref:
        description: '要构建的分支/tag/commit'
        required: false
        default: ''
  # push:
  #   tags:
  #     - 'v*'
  #     - 'release-*'

permissions:
  contents: read
  packages: write   # 推送 GHCR 镜像

jobs:
  docker:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.ref || github.ref }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Parse version from git tag
        id: version
        shell: bash
        run: |
          RAW_TAG="${{ github.ref_name }}"
          VERSION="${RAW_TAG#v}"
          VERSION="${VERSION%%-*}"
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Parsed version: $VERSION"

      # GHCR 镜像名必须全小写：github.repository 可能含大写字母（如 KleinPan/ChildNotes）
      # docker/metadata-action 内部会小写化，但后续 inspect/pull 步骤需用同样的小写形式
      - name: Compute lowercase image name
        id: image
        shell: bash
        run: |
          REPO_LC=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
          IMAGE="ghcr.io/${REPO_LC}/api"
          echo "image=$IMAGE" >> $GITHUB_OUTPUT
          echo "Computed image: $IMAGE"

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ steps.image.outputs.image }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix=
            latest

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./ChildNotes.Backend/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            VERSION=${{ steps.version.outputs.version }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64
          # 单平台构建时加载到本地 docker daemon，供后续验证步骤 inspect
          # 注意：多平台时需移除此项并改用 docker pull + inspect
          load: true

      - name: Verify Docker image version
        shell: bash
        run: |
          EXPECTED="${{ steps.version.outputs.version }}"
          IMAGE="${{ steps.image.outputs.image }}:latest"

          # buildx 推送后本地无镜像，load:true 已将单平台镜像加载到本地 daemon
          # 若 inspect 仍失败，回退到 pull 远程镜像
          if ! docker inspect "$IMAGE" >/dev/null 2>&1; then
            echo "本地未找到镜像，尝试拉取..."
            docker pull "$IMAGE"
          fi

          # 从镜像标签中读取版本号
          ACTUAL=$(docker inspect "$IMAGE" --format '{{ index .Config.Labels "org.opencontainers.image.version" }}' 2>/dev/null || echo "unknown")
          echo "Docker image version label: $ACTUAL"

          if [[ "$ACTUAL" != "$EXPECTED" ]]; then
            echo "::error::Docker 镜像版本号不匹配！期望: $EXPECTED, 实际: $ACTUAL"
            exit 1
          fi

          echo "::notice::Docker 镜像版本号验证通过: $ACTUAL"

      - name: Summary
        if: always()
        shell: bash
        run: |
          echo "### Docker 镜像已推送至 GHCR" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "版本号: ${{ steps.version.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "${{ steps.meta.outputs.tags }}" >> $GITHUB_STEP_SUMMARY
