<UserControl x:Class="ChildNotes.Views.GrowthView"
             xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ctrl="using:ChildNotes.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:v="using:ChildNotes.Views"
             xmlns:vm="using:ChildNotes.ViewModels"
             d:DesignHeight="844"
             d:DesignWidth="390"
             x:DataType="vm:GrowthViewModel"
             x:CompileBindings="True"
             mc:Ignorable="d">

    <UserControl.Styles>
        <!--  日记卡片：成长记录条目（冷调灰绿色系，与暖橘色时间轴形成视觉层次）  -->
        <Style Selector="Border.growth-card">
            <Setter Property="Background" Value="{StaticResource SurfaceCardBrush}" />
            <!-- 圆角对齐 design-tokens radius.medium=12 -->
            <Setter Property="CornerRadius" Value="{StaticResource Radius.Medium}" />
            <Setter Property="Padding" Value="14,12" />
            <Setter Property="BorderBrush" Value="{StaticResource GrowthCardBorderBrush}" />
            <Setter Property="BorderThickness" Value="3,0,0,0" />
            <!-- 阴影对齐 design-tokens shadow.card -->
            <Setter Property="BoxShadow" Value="{StaticResource Shadow.Card}" />
        </Style>
        <Style Selector="Border.growth-card-add">
            <Setter Property="Background" Value="{StaticResource SurfaceBackgroundBrush}" />
            <!-- 圆角对齐 design-tokens radius.medium=12 -->
            <Setter Property="CornerRadius" Value="{StaticResource Radius.Medium}" />
            <Setter Property="Padding" Value="14,12" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderDefaultBrush}" />
            <Setter Property="BorderThickness" Value="3,0,0,0" />
        </Style>
        <!--  标题：深棕黑、15px SemiBold，视觉权重最高  -->
        <Style Selector="TextBlock.growth-title">
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource GrowthBrownDeepBrush}" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <!--  日期徽章：橘粉色背景白字（与左侧时间轴同色系，呼应时间轴主题）  -->
        <Style Selector="Border.growth-date-badge">
            <Setter Property="Background" Value="{StaticResource GrowthPeachDeepBrush}" />
            <Setter Property="CornerRadius" Value="{StaticResource Radius.Small}" />
            <Setter Property="Padding" Value="8,3" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style Selector="TextBlock.growth-date">
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Foreground" Value="{StaticResource SurfaceCardBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Right" />
        </Style>
        <!--  正文：浅棕灰、13px Regular，与标题形成层级差异  -->
        <Style Selector="TextBlock.growth-content">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="Regular" />
            <Setter Property="Foreground" Value="{StaticResource GrowthBrownTextBrush}" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="LineHeight" Value="20" />
        </Style>
        <!--  时间轴圆点：暖橘色系  -->
        <Style Selector="Border.timeline-dot">
            <Setter Property="Width" Value="14" />
            <Setter Property="Height" Value="14" />
            <Setter Property="CornerRadius" Value="7" />
            <Setter Property="Background" Value="{StaticResource GrowthPeachBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource SurfaceCardBrush}" />
            <Setter Property="BorderThickness" Value="3" />
            <Setter Property="BoxShadow" Value="{StaticResource Shadow.Card}" />
        </Style>
        <Style Selector="Border.timeline-dot-add">
            <Setter Property="Width" Value="14" />
            <Setter Property="Height" Value="14" />
            <Setter Property="CornerRadius" Value="7" />
            <Setter Property="Background" Value="{StaticResource SurfaceCardBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}" />
            <Setter Property="BorderThickness" Value="2" />
        </Style>

        <!--  照片删除按钮：圆形半透明黑底白字（对齐小程序 .g-photo-del）。
              覆盖 Button 默认主题，避免出现边框/默认背景。  -->
        <Style Selector="Button.photo-del-btn">
            <Setter Property="Background" Value="{StaticResource MaskBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="CornerRadius" Value="11" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="MinWidth" Value="0" />
            <Setter Property="MinHeight" Value="0" />
            <Setter Property="Foreground" Value="{StaticResource SurfaceCardBrush}" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style Selector="Button.photo-del-btn:pointerover /template/ ContentPresenter">
            <Setter Property="Background" Value="{StaticResource OverlayScrimBrush}" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
        <Style Selector="Button.photo-del-btn:pressed /template/ ContentPresenter">
            <Setter Property="Background" Value="{StaticResource MaskBrush}" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>

        <!--  照片添加按钮：虚线边框浅灰底（对齐小程序 .g-photo-add）  -->
        <Style Selector="Button.photo-add-btn">
            <Setter Property="Background" Value="{StaticResource SurfaceBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderDefaultBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="{StaticResource Radius.Small}" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="MinWidth" Value="0" />
            <Setter Property="MinHeight" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style Selector="Button.photo-add-btn:pointerover /template/ ContentPresenter">
            <Setter Property="Background" Value="{StaticResource SurfaceDisabledBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}" />
        </Style>
    </UserControl.Styles>

    <Panel>
        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <StackPanel Margin="0,40,0,32" HorizontalAlignment="Center">
                    <TextBlock Margin="0,0,0,8" HorizontalAlignment="Center" FontSize="40" Text="📆" />
                    <TextBlock HorizontalAlignment="Center" FontSize="{StaticResource FontSize.CardTitle}" FontWeight="Bold" Foreground="{StaticResource GrowthBrownPrimaryBrush}"
                               Text="{DynamicResource Growth_Title}" />
                    <TextBlock Margin="0,4,0,0" HorizontalAlignment="Center" FontSize="{StaticResource FontSize.Caption}" Foreground="{StaticResource GrowthBrownLightBrush}"
                               Text="{DynamicResource Growth_Subtitle}" />
                </StackPanel>

                <ItemsControl ItemsSource="{Binding Milestones}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="vm:MilestoneDisplayItem">
                            <Grid Margin="16,0,16,20" ColumnDefinitions="32,*">
                                <ctrl:TimelineAxisItem Grid.Column="0" DotBorderBrush="{StaticResource SurfaceCardBrush}" DotBorderThickness="3" DotBrush="{StaticResource GrowthPeachBrush}"
                                                       DotSize="14" DotTopMargin="10" HalfSpacing="10" LineBrush="{StaticResource GrowthPeachBrush}"
                                                       LineWidth="2" />
                                <Border Grid.Column="1" Margin="8,0,0,0" Classes="growth-card" PointerPressed="OnMilestoneTap"
                                        Tag="{Binding .}">
                                    <StackPanel Spacing="10">
                                        <Grid ColumnDefinitions="*,Auto">
                                            <TextBlock Grid.Column="0" Classes="growth-title" Text="{Binding Title}" />
                                            <Border Grid.Column="1" Classes="growth-date-badge">
                                                <TextBlock Classes="growth-date" Text="{Binding DateText}" />
                                            </Border>
                                        </Grid>
                                        <Border Padding="10,8" Background="{StaticResource SurfaceDisabledBrush}" CornerRadius="{StaticResource Radius.XS}" IsVisible="{Binding Content, Converter={x:Static StringConverters.IsNotNullOrEmpty}}">
                                            <TextBlock Classes="growth-content" Text="{Binding Content}" />
                                        </Border>
                                        <!--  照片缩略图网格（最多 4 张，本地路径同步加载，远程 URL 异步加载）  -->
                                        <ItemsControl IsVisible="{Binding HasPhotos}" ItemsSource="{Binding PhotoThumbnails}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel />
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate x:DataType="vm:MilestoneThumbItem">
                                                    <Border Width="72" Height="72" Margin="0,0,6,6" Background="{StaticResource SurfaceDisabledBrush}"
                                                            CornerRadius="{StaticResource Radius.Small}" ClipToBounds="True"
                                                            PointerPressed="OnPhotoTap">
                                                        <Image Source="{Binding Bitmap}" Stretch="UniformToFill" />
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Grid Margin="16,0,16,40" ColumnDefinitions="32,*">
                    <ctrl:TimelineAxisItem Grid.Column="0" DotBorderBrush="{StaticResource BorderSubtleBrush}" DotBorderThickness="2" DotBrush="{StaticResource SurfaceCardBrush}"
                                           DotSize="14" DotTopMargin="10" HalfSpacing="20" LineBrush="{StaticResource BorderSubtleBrush}"
                                           LineWidth="2" />
                    <Border Grid.Column="1" Margin="8,0,0,0" Classes="growth-card-add" PointerPressed="OnAddMilestone">
                        <TextBlock HorizontalAlignment="Center" FontSize="{StaticResource FontSize.Label}" Foreground="{StaticResource GrowthHintGrayBrush}" Text="{DynamicResource Growth_AddMoment}" />
                    </Border>
                </Grid>

                <Border MaxWidth="280" Margin="16,8,16,40" Padding="24,28" HorizontalAlignment="Center"
                        Background="{StaticResource GrowthCreamBrush}" CornerRadius="{StaticResource Radius.Small}"
                        IsVisible="{Binding Milestones.Count, Converter={x:Static ObjectConverters.Equal}, ConverterParameter=0}">
                    <StackPanel HorizontalAlignment="Center" Spacing="10">
                        <TextBlock HorizontalAlignment="Center" FontSize="36" Text="🌱" />
                        <TextBlock HorizontalAlignment="Center" FontSize="{StaticResource FontSize.Body}" FontWeight="SemiBold" Foreground="{StaticResource GrowthBrownSecondaryBrush}"
                                   Text="{DynamicResource Growth_EmptyTitle}" />
                        <TextBlock HorizontalAlignment="Center" FontSize="{StaticResource FontSize.Caption}" Foreground="{StaticResource GrowthBrownLightBrush}" Text="{DynamicResource Growth_EmptyHint1}"
                                   TextAlignment="Center" />
                        <TextBlock HorizontalAlignment="Center" FontSize="{StaticResource FontSize.Caption}" Foreground="{StaticResource GrowthBrownLightBrush}" Text="{DynamicResource Growth_EmptyHint2}"
                                   TextAlignment="Center" />
                        <Button Margin="0,8,0,0" Classes="weui-btn primary" Command="{Binding AddMilestoneCommand}" Content="{DynamicResource Growth_AddNow}" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!--  编辑/新增成长记录的底部弹层  -->
        <Panel x:Name="MilestoneEditPanel" IsVisible="{Binding MilestoneEdit.IsVisible}">
            <Border Classes="modal-mask" />

            <!--  Sheet 弹层  -->
            <Border MaxHeight="720" VerticalAlignment="Bottom" Background="{StaticResource SurfaceCardBrush}" CornerRadius="24,24,0,0">
                <Grid RowDefinitions="Auto,*,Auto">
                    <!--  头部（固定）  -->
                    <Border Grid.Row="0" Padding="16,12" BorderBrush="{StaticResource SurfaceDividerBrush}" BorderThickness="0,0,0,1">
                        <Grid ColumnDefinitions="*,Auto">
                            <TextBlock Grid.Column="0" VerticalAlignment="Center" FontSize="{StaticResource FontSize.CardTitle}" FontWeight="Bold"
                                       Text="{Binding MilestoneEdit.SheetTitle}" />
                            <Button Grid.Column="1" Padding="8,2" Background="Transparent" BorderThickness="0"
                                    Command="{Binding MilestoneEdit.CloseCommand}"
                                    Content="✕" FontSize="18" />
                        </Grid>
                    </Border>

                    <!--  表单内容（可滚动）  -->
                    <ScrollViewer Grid.Row="1" MinHeight="300" HorizontalScrollBarVisibility="Disabled"
                                  VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="16,16" Spacing="16">
                            <!--
                                日期选择放在最前面：DatePicker 弹出面板向下展开，
                                放在表单顶部可确保展开时有充足空间，不会被底部保存按钮遮挡
                            -->
                            <StackPanel>
                                <TextBlock Margin="0,0,0,8" FontSize="{StaticResource FontSize.Label}" Foreground="{StaticResource TextSecondaryBrush}" Text="{DynamicResource Growth_EditDate}" />
                                <ctrl:DateWheelPicker HorizontalAlignment="Stretch"
                                                      SelectedDate="{Binding MilestoneEdit.RecordDate, Mode=TwoWay}" />
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Margin="0,0,0,8" FontSize="{StaticResource FontSize.Label}" Foreground="{StaticResource TextSecondaryBrush}" Text="{DynamicResource Growth_EditTitle}" />
                                <TextBox x:Name="MilestoneTitleTextBox" Padding="12,8" Background="{StaticResource SurfaceDisabledBrush}" CornerRadius="{StaticResource Radius.Small}" PlaceholderText="{DynamicResource Growth_EditTitlePlaceholder}"
                                         Text="{Binding MilestoneEdit.Title}" />
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Margin="0,0,0,8" FontSize="{StaticResource FontSize.Label}" Foreground="{StaticResource TextSecondaryBrush}" Text="{DynamicResource Growth_EditContentLabel}" />
                                <TextBox Height="100" Padding="12,8" VerticalContentAlignment="Top" AcceptsReturn="True"
                                         Background="{StaticResource SurfaceDisabledBrush}" CornerRadius="{StaticResource Radius.Small}" PlaceholderText="{DynamicResource Growth_EditContentPlaceholder}"
                                         Text="{Binding MilestoneEdit.Content}"
                                         TextWrapping="Wrap" />
                            </StackPanel>
                            <!--  照片编辑区：网格 + 添加按钮，最多 4 张  -->
                            <StackPanel>
                                <Grid ColumnDefinitions="*,Auto" Margin="0,0,0,8">
                                    <TextBlock Grid.Column="0" FontSize="{StaticResource FontSize.Label}" Foreground="{StaticResource TextSecondaryBrush}" Text="{DynamicResource Growth_EditPhotosLabel}" />
                                    <TextBlock Grid.Column="1" VerticalAlignment="Center" FontSize="{StaticResource FontSize.Caption}"
                                               Foreground="{StaticResource TextPlaceholderBrush}"
                                               IsVisible="{Binding MilestoneEdit.CanAddPhoto}"
                                               Text="{DynamicResource Growth_EditAddPhotoHint}" />
                                </Grid>
                                <WrapPanel>
                                    <ItemsControl HorizontalAlignment="Left" ItemsSource="{Binding MilestoneEdit.Photos}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate x:DataType="vm:MilestonePhotoItem">
                                                <Grid Width="80" Height="80" Margin="0,0,8,8">
                                                    <Border CornerRadius="{StaticResource Radius.Small}" ClipToBounds="True">
                                                        <!--  本地路径直接绑定 Bitmap；远程 URL 用 Image 异步加载  -->
                                                        <Image Source="{Binding Bitmap}" Stretch="UniformToFill" />
                                                    </Border>
                                                    <!--  上传中指示器  -->
                                                    <Border Background="{StaticResource MaskLightBrush}" CornerRadius="{StaticResource Radius.Small}" IsVisible="{Binding IsUploading}">
                                                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                   FontSize="{StaticResource FontSize.Caption}" Foreground="{StaticResource SurfaceCardBrush}" Text="{DynamicResource Growth_Uploading}" />
                                                    </Border>
                                                    <!--  删除按钮（右上角内侧，对齐小程序 top:4rpx right:4rpx）  -->
                                                <Button Width="22" Height="22" Margin="0,3,3,0"
                                                        HorizontalAlignment="Right" VerticalAlignment="Top"
                                                        Classes="photo-del-btn"
                                                        Click="OnRemovePhotoClick"
                                                        Tag="{Binding .}"
                                                        Content="✕" FontSize="11" />
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <!--  添加按钮：与照片同处一个 WrapPanel，跟随最后一张照片右侧；达到上限时隐藏  -->
                                    <Button Width="80" Height="80" Margin="0,0,0,8"
                                            Classes="photo-add-btn"
                                            Click="OnAddPhotoClick"
                                            IsVisible="{Binding MilestoneEdit.CanAddPhoto}">
                                        <TextBlock FontSize="28" Foreground="{StaticResource GrowthHintGrayBrush}" Text="+" />
                                    </Button>
                                </WrapPanel>
                            </StackPanel>
                            <TextBlock FontSize="{StaticResource FontSize.Label}" Foreground="{StaticResource SemanticErrorBrush}" IsVisible="{Binding MilestoneEdit.ErrorMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" Text="{Binding MilestoneEdit.ErrorMessage}" />
                        </StackPanel>
                    </ScrollViewer>

                    <!--  底部按钮栏（固定）  -->
                    <Border Grid.Row="2" Padding="16,12" BorderBrush="{StaticResource SurfaceDividerBrush}" BorderThickness="0,1,0,0">
                        <Grid ColumnDefinitions="*,Auto">
                            <Button Grid.Column="0" HorizontalAlignment="Stretch" Classes="weui-btn primary"
                                    Command="{Binding MilestoneEdit.SaveCommand}"
                                    Content="{DynamicResource Common_Save}" />
                            <Button Grid.Column="1" Margin="8,0,0,0" Classes="weui-btn warn"
                                    Command="{Binding MilestoneEdit.DeleteCommand}"
                                    Content="{DynamicResource Common_Delete}"
                                    IsVisible="{Binding MilestoneEdit.SheetTitle, Converter={x:Static v:GrowthView.IsEditingConverter}}" />
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Panel>

        <!--  全屏图片预览弹窗（点击缩略图触发）  -->
        <Panel IsVisible="{Binding IsPreviewOpen}" Background="{StaticResource MaskBrush}"
               PointerPressed="OnPreviewBackgroundTap">
            <Grid>
                <Image Source="{Binding PreviewCurrentBitmap}"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Stretch="Uniform" Margin="24" />
                <!--  关闭按钮  -->
                <Button Width="44" Height="44" Margin="0,24,24,0"
                        HorizontalAlignment="Right" VerticalAlignment="Top"
                        Background="{StaticResource GrowthSemiWhiteBrush}" BorderThickness="0" CornerRadius="{StaticResource Radius.Pill}"
                        Command="{Binding ClosePreviewCommand}"
                        Content="✕" FontSize="18" Foreground="Black" />
                <!--  上一张  -->
                <Button Width="44" Height="44" Margin="24,0"
                        HorizontalAlignment="Left" VerticalAlignment="Center"
                        Background="{StaticResource GrowthSemiWhiteBrush}" BorderThickness="0" CornerRadius="{StaticResource Radius.Pill}"
                        Command="{Binding PreviewPrevCommand}"
                        Content="‹" FontSize="24" Foreground="Black" />
                <!--  下一张  -->
                <Button Width="44" Height="44" Margin="24,0"
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Background="{StaticResource GrowthSemiWhiteBrush}" BorderThickness="0" CornerRadius="{StaticResource Radius.Pill}"
                        Command="{Binding PreviewNextCommand}"
                        Content="›" FontSize="24" Foreground="Black" />
                <!--  计数  -->
                <Border Margin="0,0,0,24" Padding="12,6"
                        HorizontalAlignment="Center" VerticalAlignment="Bottom"
                        Background="{StaticResource GrowthSemiBlackBrush}" CornerRadius="{StaticResource Radius.Medium}">
                    <TextBlock Foreground="{StaticResource SurfaceCardBrush}" FontSize="{StaticResource FontSize.Label}"
                               Text="{Binding PreviewCountText}" />
                </Border>
            </Grid>
        </Panel>

    </Panel>
</UserControl>
