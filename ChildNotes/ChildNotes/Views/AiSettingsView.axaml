<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:ChildNotes.ViewModels"
             xmlns:v="using:ChildNotes.Views"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="390" d:DesignHeight="844"
             x:Class="ChildNotes.Views.AiSettingsView"
             x:DataType="vm:AiSettingsViewModel"
             Background="{StaticResource SurfaceBackgroundBrush}">

    <Grid RowDefinitions="Auto,*,Auto">

        <!-- 顶栏 -->
        <Border Grid.Row="0" Classes="nav-bar">
            <Grid ColumnDefinitions="Auto,*,Auto">
                <Button Grid.Column="0" Classes="nav-back" Command="{Binding BackCommand}"/>
                <TextBlock Grid.Column="1" Classes="nav-title" Text="{DynamicResource AiSettings_Title}"/>
                <TextBlock Grid.Column="2" Text=" " Width="40"/>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <!-- 启用开关 -->
                <Border Background="{StaticResource SurfaceCardBrush}" CornerRadius="{StaticResource Radius.Medium}" Margin="12,12,12,4">
                    <Grid Margin="14,12" ColumnDefinitions="*,Auto">
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{DynamicResource AiSettings_EnableAi}" FontSize="{StaticResource FontSize.Body}"/>
                            <TextBlock Text="{DynamicResource AiSettings_EnableAiHint}" FontSize="{StaticResource FontSize.Caption}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,0"/>
                        </StackPanel>
                        <ToggleSwitch Grid.Column="1" IsChecked="{Binding Enabled}"/>
                    </Grid>
                </Border>

                <!-- 模型配置 -->
                <Border Classes="section-title">
                    <TextBlock Text="{DynamicResource AiSettings_ModelSection}"/>
                </Border>

                <Border Background="{StaticResource SurfaceCardBrush}" CornerRadius="{StaticResource Radius.Medium}" Margin="12,4">
                    <StackPanel>
                        <Grid Margin="14,10" ColumnDefinitions="80,*">
                            <TextBlock Grid.Column="0" Text="API 地址" FontSize="{StaticResource FontSize.Label}" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Text="{Binding ConfigApiBaseUrl}" PlaceholderText="https://api.openai.com"
                                     Background="Transparent" BorderThickness="0"/>
                        </Grid>
                        <Border Height="1" Background="{StaticResource SurfaceDividerBrush}" Margin="14,0"/>
                        <Grid Margin="14,10" ColumnDefinitions="80,*">
                            <TextBlock Grid.Column="0" Text="API Key" FontSize="{StaticResource FontSize.Label}" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Text="{Binding ConfigApiKey}" PlaceholderText="sk-..."
                                     PasswordChar="•" Background="Transparent" BorderThickness="0"/>
                        </Grid>
                        <Border Height="1" Background="{StaticResource SurfaceDividerBrush}" Margin="14,0"/>
                        <Grid Margin="14,10" ColumnDefinitions="80,*">
                            <TextBlock Grid.Column="0" Text="{DynamicResource AiSettings_ModelName}" FontSize="{StaticResource FontSize.Label}" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Text="{Binding ConfigModelName}" PlaceholderText="gpt-4o-mini"
                                     Background="Transparent" BorderThickness="0"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- "Ai记"解析服务来源 -->
                <Border Classes="section-title">
                    <TextBlock Text="{DynamicResource AiSettings_ParseService}"/>
                </Border>

                <Border Background="{StaticResource SurfaceCardBrush}" CornerRadius="{StaticResource Radius.Medium}" Margin="12,4">
                    <StackPanel>
                        <Grid Margin="14,12" ColumnDefinitions="*,Auto">
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{DynamicResource AiSettings_SourceLabel}" FontSize="{StaticResource FontSize.Label}"/>
                                <TextBlock Text="{DynamicResource AiSettings_SourceHint}" FontSize="{StaticResource FontSize.Caption}"
                                           Foreground="{StaticResource TextPlaceholderBrush}" Margin="0,2,0,0" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Grid>
                        <Grid Margin="14,0,14,12" ColumnDefinitions="*,*">
                            <RadioButton Grid.Column="0" Content="{DynamicResource AiSettings_LocalModel}" IsChecked="{Binding UseLocalSource, Mode=TwoWay}"
                                         IsEnabled="{Binding !IsTesting}"/>
                            <RadioButton Grid.Column="1" Content="{DynamicResource AiSettings_ServerService}" IsChecked="{Binding UseServerSource, Mode=TwoWay}"
                                         IsEnabled="{Binding !IsTesting}"/>
                        </Grid>
                        <Border Height="1" Background="{StaticResource SurfaceDividerBrush}" Margin="14,0"/>
                        <Grid Margin="14,10" ColumnDefinitions="80,*" IsVisible="{Binding IsServerSource}">
                            <TextBlock Grid.Column="0" Text="{DynamicResource AiSettings_BackendAddr}" FontSize="{StaticResource FontSize.Label}" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="{Binding ServerUrl}" FontSize="{StaticResource FontSize.Label}"
                                       Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"/>
                        </Grid>
                        <Grid Margin="14,10" ColumnDefinitions="80,*" IsVisible="{Binding IsServerSource}">
                            <TextBlock Grid.Column="0" Text="{DynamicResource AiSettings_Hint}" FontSize="{StaticResource FontSize.Label}" VerticalAlignment="Top"/>
                            <TextBlock Grid.Column="1" Text="{DynamicResource AiSettings_BackendHint}" FontSize="{StaticResource FontSize.Caption}"
                                       Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 智能识别模式（仅后端服务来源下生效） -->
                <Border Classes="section-title" IsVisible="{Binding IsServerSource}">
                    <TextBlock Text="{DynamicResource AiSettings_ParseMode}"/>
                </Border>

                <Border Background="{StaticResource SurfaceCardBrush}" CornerRadius="{StaticResource Radius.Medium}" Margin="12,4" IsVisible="{Binding IsServerSource}">
                    <StackPanel>
                        <Grid Margin="14,12" ColumnDefinitions="*,Auto">
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{DynamicResource AiSettings_ParseModeLabel}" FontSize="{StaticResource FontSize.Label}"/>
                                <TextBlock Text="{DynamicResource AiSettings_ParseModeHint}" FontSize="{StaticResource FontSize.Caption}"
                                           Foreground="{StaticResource TextPlaceholderBrush}" Margin="0,2,0,0" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Grid>
                        <Grid Margin="14,0,14,12" ColumnDefinitions="*,*">
                            <RadioButton Grid.Column="0" Content="{DynamicResource AiSettings_ParseModeFast}" IsChecked="{Binding UseFastMode, Mode=TwoWay}"
                                         IsEnabled="{Binding !IsTesting}"/>
                            <RadioButton Grid.Column="1" Content="{DynamicResource AiSettings_ParseModePrecise}" IsChecked="{Binding UsePreciseMode, Mode=TwoWay}"
                                         IsEnabled="{Binding !IsTesting}"/>
                        </Grid>
                        <Border Height="1" Background="{StaticResource SurfaceDividerBrush}" Margin="14,0"/>
                        <Grid Margin="14,10" ColumnDefinitions="80,*" IsVisible="{Binding UsePreciseMode}">
                            <TextBlock Grid.Column="0" Text="{DynamicResource AiSettings_Hint}" FontSize="{StaticResource FontSize.Label}" VerticalAlignment="Top"/>
                            <TextBlock Grid.Column="1" Text="{DynamicResource AiSettings_ParseModePreciseHint}" FontSize="{StaticResource FontSize.Caption}"
                                       Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 连接测试 -->
                <Button Content="{Binding TestButtonText}" Classes="btn secondary large" Margin="12,12"
                        HorizontalAlignment="Stretch"
                        Command="{Binding TestConnectionCommand}"
                        IsEnabled="{Binding !IsTesting}"/>

                <Border CornerRadius="{StaticResource Radius.Small}" Padding="10,8" Margin="12,4"
                        Background="{Binding TestSuccess, Converter={x:Static v:AppConverters.TestResultBackgroundConverter}}"
                        IsVisible="{Binding TestResult, Converter={x:Static StringConverters.IsNotNullOrEmpty}}">
                    <StackPanel Spacing="2">
                        <TextBlock FontSize="{StaticResource FontSize.Label}" FontWeight="Medium"
                                   Foreground="{Binding TestSuccess, Converter={x:Static v:AppConverters.TestResultForegroundConverter}}"
                                   Text="{Binding TestSuccess, Converter={x:Static v:AppConverters.TestResultTitleConverter}}"/>
                        <TextBlock Text="{Binding TestResult}" FontSize="{StaticResource FontSize.Caption}" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- 生成参数 -->
                <Border Classes="section-title">
                    <TextBlock Text="{DynamicResource AiSettings_ParamSection}"/>
                </Border>

                <Border Background="{StaticResource SurfaceCardBrush}" CornerRadius="{StaticResource Radius.Medium}" Margin="12,4">
                    <StackPanel Margin="14,12">
                        <Grid ColumnDefinitions="*,Auto" Margin="0,0,0,6">
                            <TextBlock Grid.Column="0" Text="{DynamicResource AiSettings_Temperature}" FontSize="{StaticResource FontSize.Label}"/>
                            <TextBlock Grid.Column="1" Text="{Binding ConfigTemperature, StringFormat=\{0:F1\}}" FontSize="{StaticResource FontSize.Label}" Foreground="{StaticResource TextSecondaryBrush}"/>
                        </Grid>
                        <Slider Minimum="0" Maximum="2" Value="{Binding ConfigTemperature}" TickFrequency="0.1" IsSnapToTickEnabled="True" Margin="0,0,0,12"/>

                        <Grid ColumnDefinitions="*,Auto" Margin="0,0,0,6">
                            <TextBlock Grid.Column="0" Text="{DynamicResource AiSettings_MaxTokens}" FontSize="{StaticResource FontSize.Label}"/>
                            <TextBlock Grid.Column="1" Text="{Binding ConfigMaxTokens}" FontSize="{StaticResource FontSize.Label}" Foreground="{StaticResource TextSecondaryBrush}"/>
                        </Grid>
                        <Slider Minimum="512" Maximum="8192" Value="{Binding ConfigMaxTokens}" TickFrequency="256" IsSnapToTickEnabled="True"/>
                    </StackPanel>
                </Border>

                <Button Content="{DynamicResource AiSettings_Save}" Classes="btn primary large" Margin="12,16" HorizontalAlignment="Stretch"
                        Command="{Binding SaveCommand}"/>

                <!-- 说明 -->
                <Border Classes="section-title">
                    <TextBlock Text="{DynamicResource AiSettings_Description}"/>
                </Border>
                <Border Background="{StaticResource SurfaceCardBrush}" CornerRadius="{StaticResource Radius.Medium}" Margin="12,4,12,24">
                    <StackPanel Margin="14,12" Spacing="6">
                        <TextBlock Text="{DynamicResource AiSettings_Note1}" FontSize="{StaticResource FontSize.Caption}" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
                        <TextBlock Text="{DynamicResource AiSettings_Note2}" FontSize="{StaticResource FontSize.Caption}" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
                        <TextBlock Text="{DynamicResource AiSettings_Note3}" FontSize="{StaticResource FontSize.Caption}" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
                        <TextBlock Text="{DynamicResource AiSettings_Note4}" FontSize="{StaticResource FontSize.Caption}" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
                        <TextBlock Text="{DynamicResource AiSettings_Note5}" FontSize="{StaticResource FontSize.Caption}" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
                        <TextBlock Text="{DynamicResource AiSettings_Note6}" FontSize="{StaticResource FontSize.Caption}" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="1" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,80"
                Background="{StaticResource MaskBrush}" CornerRadius="{StaticResource Radius.Small}" Padding="20,10" IsVisible="{Binding ShowToast}">
            <TextBlock Text="{Binding ToastMessage}" Foreground="{StaticResource SurfaceCardBrush}" FontSize="{StaticResource FontSize.Label}" TextWrapping="Wrap" MaxWidth="320"/>
        </Border>
    </Grid>
</UserControl>
